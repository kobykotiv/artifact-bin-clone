// Notification and activity feed service (stub)
export function subscribeToNotifications(userId: string, cb: (notif: any) => void) {
  // Listen for notifications for userId
}

export function getActivityFeed(userId: string) {
  // Return recent activity for userId
  return [];
}
