import React from 'react';

export function ActivityFeed({ userId }: { userId: string }) {
  // Replace with real activity feed logic
  return (
    <div className="activity-feed">
      <h4>Recent Activity</h4>
      <ul>
        <li>No recent activity.</li>
      </ul>
    </div>
  );
}
